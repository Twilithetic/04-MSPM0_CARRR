# MSPM0G3507 巡线小车项目 — 编译与烧录指南

## 一、问题概述

**项目**: MSPM0G3507 5通道灰度循迹小车
**需求**: 如何编译代码并烧录到 LP-MSPM0G3507 LaunchPad 开发板

---

## 二、项目分析

### 2.1 硬件平台
| 项目 | 详情 |
|------|------|
| 芯片 | MSPM0G3507 (Arm Cortex-M0+, 32-bit) |
| 开发板 | LP-MSPM0G3507 LaunchPad |
| 调试器 | XDS110 (板载 USB，通过 SWD 接口) |

### 2.2 软件环境 (已确认安装)
| 组件 | 路径 |
|------|------|
| CCS Theia IDE | `C:\ti\ccs2100\ccs\theia\ccstudio.exe` |
| CCS 命令行服务器 | `C:\ti\ccs2100\ccs\eclipse\ccs-server.exe` |
| TI Arm Clang 工具链 | `C:\ti\ti_cgt_arm_llvm_4.0.2.LTS\bin\tiarmclang.exe` |
| MSPM0 SDK | `C:\ti\mspm0_sdk_2_10_00_04` |

### 2.3 编译产物 (已存在)
| 文件 | 说明 |
|------|------|
| `Debug/empty.out` | ELF 可执行文件 (187KB) — **烧录用这个** |
| `Debug/empty.hex` | Intel Hex 文件 (35KB) |
| `Debug/empty.map` | 内存映射文件 |

### 2.4 关键发现
- ✅ **代码已经编译过了**，`Debug/` 目录下有现成的 `.out` 和 `.hex` 文件
- ⚠️ `.cproject` 中 origin 指向 `E:\softWare\mspm0\mspm0_sdk_2_10_00_04`，但实际 SDK 在 `C:\ti\mspm0_sdk_2_10_00_04`，用 CCS 重新编译前需要更新 SDK 路径
- ⚠️ `.cproject` 中指定的工具链版本是 `TICLANG_5.1.1.LTS`，而实际安装的是 `4.0.2.LTS`，CCS 可能会自动处理版本兼容

---

## 三、解决方案

### 方案 A：CCS Theia GUI 编译+烧录（推荐、最简单）

#### A1. 打开项目
1. 启动 CCS Theia：双击 `C:\ti\ccs2100\ccs\theia\ccstudio.exe`
2. 选择工作空间（workspace），建议用 `D:\04-NUEDC_prepare`
3. 菜单 → File → Import Project → 浏览到 `D:\04-NUEDC_prepare\empty`

#### A2. 编译（可选，如果想修改代码后重新编译）
- 菜单 → Project → Build Project (或按 `Ctrl+B`)
- 编译输出在 `Debug/` 目录

#### A3. 烧录（Debug 模式）
1. 用 USB 线连接 LP-MSPM0G3507 到电脑
2. 菜单 → Run → Debug (或按 `F11`)
3. CCS 自动编译 → 烧录 → 进入调试模式
4. 按 `F8` (Resume) 运行程序

### 方案 B：命令行编译

```powershell
# 使用 CCS Server 命令行编译
& "C:\ti\ccs2100\ccs\eclipse\ccs-server.exe" `
  -noSplash `
  -data "D:\04-NUEDC_prepare" `
  -application com.ti.ccs.apps.buildProject `
  -ccs.projects empty `
  -ccs.configuration Debug
```

### 方案 C：probe-rs 命令行烧录 ⭐ 推荐！极速！

**probe-rs 完全支持 MSPM0G3507 + XDS110！** 你的电脑已安装 probe-rs 0.31.0。

#### C1. 一键烧录（最快）
```powershell
# 下载烧录（程序烧完后保持复位，需手动复位或重新上电运行）
probe-rs download --chip MSPM0G3507 --protocol swd `
  --probe 0451:bef3-2:MG350001 `
  "D:\04-NUEDC_prepare\empty\Debug\empty.out"

# 烧录并立即运行
probe-rs run --chip MSPM0G3507 --protocol swd `
  --probe 0451:bef3-2:MG350001 `
  "D:\04-NUEDC_prepare\empty\Debug\empty.out"

# 查看芯片信息
probe-rs info --chip MSPM0G3507 --protocol swd `
  --probe 0451:bef3-2:MG350001

# 擦除全部 flash
probe-rs erase --chip MSPM0G3507 --protocol swd `
  --probe 0451:bef3-2:MG350001

# 查看已连接的调试器
probe-rs list
```

#### C2. probe-rs 支持详情

| 项目 | 状态 |
|------|------|
| 版本 | ✅ probe-rs 0.31.0 已安装 |
| XDS110 | ✅ 两个 CMSIS-DAP 接口，均可使用 |
| MSPM0G3507 | ✅ 在官方 target 列表中 (MSPM0G1X0X_G3X0X Series) |
| 烧录速度 | ✅ 0.88秒完成（128KB flash） |
| SWD 协议 | ✅ 支持 |
| 退出码 | ✅ 0（成功） |

> **来源**：probe-rs 从 PR #3106 开始支持 MSPM0G151x/G351x 系列（包含 G3507），
> 从 PR #1789 开始支持 MSPM0L 系列。

#### C3. 通过 CCS GUI 直接烧录
1. 启动 CCS Theia
2. 菜单 → View → Target Configurations
3. 右键 `MSPM0G3507.ccxml` → Launch Selected Configuration
4. 连接成功后，菜单 → Run → Load → Load Program → 选择 `Debug/empty.out`

#### C4. 通过 UniFlash 烧录
1. 下载安装 [UniFlash](https://www.ti.com/tool/UNIFLASH)
2. 选择设备 MSPM0G3507，连接 XDS110
3. 加载 `empty.out` 或 `empty.hex`
4. 点击 "Load Image"

---

## 四、技术细节

### 4.1 编译工具链详情
- **编译器**: TI Arm Clang (`tiarmclang`)，基于 LLVM/Clang
- **目标架构**: `thumbv6m` (Cortex-M0+)
- **优化级别**: `-O2` (Debug 配置)
- **调试信息**: DWARF 3 格式

### 4.2 烧录协议
- **接口**: SWD (Serial Wire Debug) 两线调试
- **引脚**: PA19 (SWDIO), PA20 (SWCLK)
- **调试器**: XDS110 (通过 USB 转 SWD)

### 4.3 SDK 路径修正（重要！）
如果要在 CCS 中重新编译，需要更新 SDK 路径：
1. 打开 CCS Theia → Preferences → Code Composer Studio → Products
2. 将 MSPM0 SDK 路径从 `E:\softWare\mspm0\mspm0_sdk_2_10_00_04` 
   改为 `C:\ti\mspm0_sdk_2_10_00_04`

### 4.4 UART 串口查看输出
程序运行后通过 UART 输出调试信息：
- TX: PA10, RX: PA11
- 波特率: 115200 8N1
- 可用串口工具 (PuTTY, Tera Term, 或 CCS 内置 Terminal) 查看

---

## 五、操作总结

### 懒人极速版（推荐！）
```powershell
# 1. 插上开发板 USB
# 2. 在终端执行：
probe-rs run --chip MSPM0G3507 --protocol swd `
  --probe 0451:bef3-2:MG350001 `
  "D:\04-NUEDC_prepare\empty\Debug\empty.out"
```
一行命令，0.88秒完成烧录+运行！✨

### CCS GUI 传统版
| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 连接开发板 | USB 线连接 LP-MSPM0G3507 到电脑 |
| 2 | 打开 CCS Theia | 双击 `C:\ti\ccs2100\ccs\theia\ccstudio.exe` |
| 3 | 导入项目 | File → Import Project → 选择 `D:\04-NUEDC_prepare\empty` |
| 4 | (可选) 重新编译 | Ctrl+B 编译 |
| 5 | 烧录运行 | F11 → F8 (Debug + Resume) |
| 6 | 查看输出 | 串口 115200 8N1 查看巡线数据 |

---

## 参考文档
- [CCS Theia 用户指南 - 命令行构建](https://software-dl.ti.com/ccs/esd/documents/users_guide_ccs_theia/ccs_project-command-line.html)
- [LP-MSPM0G3507 产品页](https://www.ti.com/tool/LP-MSPM0G3507)
- [UniFlash 快速入门](https://software-dl.ti.com/ccs/esd/uniflash/docs/v5_0/quick_start_guide/uniflash_quick_start_guide.html)
