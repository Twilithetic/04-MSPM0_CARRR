# 差速小车运动学 & 里程计 & 传感器需求

## 一、符号定义 & 实际参数

| 符号 | 含义 | 你的小车实际值 | 数据来源 |
|------|------|---------------|---------|
| r | 轮子半径 | 68/2 = **34 mm** | 常量 |
| L | 轮距（两驱动轮中心距） | ✅ **120 mm** | 实测 |
| D | 轮子直径 | **68 mm** | 常量 |
| dL | 左轮累计行驶距离 | `motor_get_distance_left_mm()` | 编码器 |
| dR | 右轮累计行驶距离 | `motor_get_distance_right_mm()` | 编码器 |
| θ | 小车朝向角 | `imu_get_yaw_deg100()` | 陀螺仪 IMU |

---

## 二、核心公式

### 2.1 逆运动学（目标速度 → 左右轮速度）

```
给定目标线速度 v (mm/s) 和角速度 ω (rad/s)：

    v_L = v - ω · L/2       左轮线速度 (mm/s)
    v_R = v + ω · L/2       右轮线速度 (mm/s)

其中 L = 120mm，所以 ω·L/2 = ω × 60
```

### 2.2 正运动学 / 里程计（传感器读数 → 位姿 x, y, θ）

```
每轮 Δt 内行驶距离（直接用 motor_get_distance 的差值）：

    Δs_L = dL(t) - dL(t-1)         左轮增量 (mm)
    Δs_R = dR(t) - dR(t-1)         右轮增量 (mm)

推算小车中心位移（编码器推算，用于 x,y）：
    Δs   = (Δs_R + Δs_L) / 2       ← 中心位移 (mm)

角度直接用 IMU（不打滑，不漂移）：
    θ = imu_get_yaw_deg100() / 100.0f × π/180    ← 当前朝向 (rad)
    Δθ = θ(t) - θ(t-1)                            ← 转角变化 (rad)

在世界坐标系中积分位姿（中点法）：
    x += Δs × cos(θ_prev + Δθ/2)
    y += Δs × sin(θ_prev + Δθ/2)
```

> 🎯 **关键优势**：θ 从 IMU 陀螺仪拿，不受轮子打滑影响！位移从编码器拿，精度够。各取所长~

---

## 三、数据采集链路

```
┌──────────────────────────────────────────────────────────┐
│  motor_get_distance_left_mm()  →  dL (mm)  每周期取差值  │
│  motor_get_distance_right_mm() →  dR (mm)  每周期取差值  │
│                              ↓                           │
│              Δs = (ΔdR + ΔdL) / 2   (中心位移)           │
│                                                          │
│  imu_get_yaw_deg100()  →  yaw_deg100                     │
│         ↓  /100 得角度(°)，×π/180 转 rad                 │
│         θ 从 IMU 直接拿，不用编码器推算                   │
└──────────────────────────────────────────────────────────┘
```

---

## 四、里程计实现（真实代码）

```c
#include <math.h>

// ========== 参数 ==========
#define WHEEL_BASE_MM       120.0f      // 轮距 (mm)
#define DEG_TO_RAD          (3.14159265f / 180.0f)

// ========== 里程计状态 ==========
static float odom_x_mm     = 0.0f;      // 世界坐标 X (mm)
static float odom_y_mm     = 0.0f;      // 世界坐标 Y (mm)
static float odom_yaw_rad  = 0.0f;      // 朝向角 (rad) — 来自 IMU
static float last_dL_mm    = 0.0f;      // 上一帧左轮距离
static float last_dR_mm    = 0.0f;      // 上一帧右轮距离
static float last_yaw_deg100 = 0.0f;    // 上一帧 IMU 角度
static bool  odom_inited   = false;

// ========== 初始化（调一次） ==========
void odometry_init(void) {
    last_dL_mm      = motor_get_distance_left_mm();
    last_dR_mm      = motor_get_distance_right_mm();
    last_yaw_deg100 = (float)imu_get_yaw_deg100();
    odom_inited     = true;
}

// ========== 每周期更新（例如每 10ms 调一次） ==========
void odometry_update(void) {
    if (!odom_inited) return;

    // ── 1. 读当前值 ──
    float dL          = motor_get_distance_left_mm();
    float dR          = motor_get_distance_right_mm();
    float yaw_deg100  = (float)imu_get_yaw_deg100();

    // ── 2. 计算增量 ──
    float ds_L = dL - last_dL_mm;           // 左轮增量 (mm)
    float ds_R = dR - last_dR_mm;           // 右轮增量 (mm)
    float ds   = (ds_R + ds_L) * 0.5f;      // 中心位移 (mm)

    // ── 3. 角度：直接用 IMU ──
    float yaw_prev = odom_yaw_rad;
    odom_yaw_rad = yaw_deg100 * 0.01f * DEG_TO_RAD;   // deg100 → rad

    // ── 4. 积分位姿（中点法） ──
    float mid_yaw = yaw_prev + (odom_yaw_rad - yaw_prev) * 0.5f;
    odom_x_mm += ds * cosf(mid_yaw);
    odom_y_mm += ds * sinf(mid_yaw);

    // ── 5. 保存上一帧 ──
    last_dL_mm      = dL;
    last_dR_mm      = dR;
    last_yaw_deg100 = yaw_deg100;
}
```

---

## 五、从目标 (x,y) 导航到目标点

```c
// 目标点 (mm)
float target_x = 500.0f;
float target_y = 300.0f;

void navigate_to_target(void) {
    // 1. 读当前位姿
    float dx = target_x - odom_x_mm;
    float dy = target_y - odom_y_mm;
    float distance   = sqrtf(dx*dx + dy*dy);
    float target_ang = atan2f(dy, dx);       // 期望朝向

    // 2. 角度误差（归一化到 [-π, π]）
    float err = target_ang - odom_yaw_rad;
    while (err >  3.14159f) err -= 6.28318f;
    while (err < -3.14159f) err += 6.28318f;

    // 3. 简单 P 控制
    float v     = 80.0f;            // 基础线速度 (mm/s)
    float omega = err * 2.5f;       // 角速度 (rad/s)，Kp=2.5
    if (fabsf(err) < 0.05f) omega = 0.0f;  // 对准后直走
    if (distance < 10.0f)  v = 0.0f;       // 到了就停

    // 4. 逆运动学 → 左右轮速度 (mm/s)
    float half_base = WHEEL_BASE_MM * 0.5f;   // 60mm
    float v_L = v - omega * half_base;
    float v_R = v + omega * half_base;

    // 5. 发送给电机
    //    注意：motor_speed() 的参数单位是编码器脉冲/10ms
    //    转换：mm/s → pulses/10ms
    //    你的电机驱动板可能是用速度闭环的，直接传 mm/s 对应的速度值
    int16_t spd_L = (int16_t)v_L;
    int16_t spd_R = (int16_t)v_R;
    drive_m2_m4(spd_L, spd_R);
}
```

---

## 六、传感器需求汇总

### 6.1 已有 ✅

| 传感器 | 采集函数 | 数据 | 用途 |
|--------|---------|------|------|
| **编码器** | `motor_get_distance_left_mm()` | 左轮累计距离 (mm) | 推算位移 Δs |
| **编码器** | `motor_get_distance_right_mm()` | 右轮累计距离 (mm) | 推算位移 Δs |
| **陀螺仪 IMU** | `imu_get_yaw_deg100()` | 朝向角 ×100 | 位姿角度 θ（不怕打滑！） |
| **灰度传感器** | `read_line_sample()` | 5通道黑白 | 巡线 |

### 6.2 IMU + 编码器融合的优势

```
纯编码器方案：        编码器 + IMU 融合方案：
  Δθ = (Δs_R-Δs_L)/L    θ = imu_get_yaw_deg100() / 100
  → 轮子打滑，θ 就歪    → IMU 不依赖轮子，绝对可靠
  → 误差无限累积        → θ 漂移极小（>10°/h 级）
  → x,y 全跑偏          → 位移仍从编码器拿，角度从 IMU 拿
```

---

## 七、参考来源

- [Columbia: Differential Drive Kinematics](https://www.cs.columbia.edu/~allen/F17/NOTES/icckinematics.pdf)
- [Robotics Book: DDR Motion Model](https://www.roboticsbook.org/S52_diffdrive_actions.html)
- [ROS2 Control: Wheeled Mobile Robot Kinematics](https://control.ros.org/rolling/doc/ros2_controllers/doc/mobile_robot_kinematics.html)
- [差速小车里程计 - 小土](https://gaoyichao.com/Xiaotu?book=Gazebo&title=差速小车的里程计)
- [GM0: Odometry](https://gm0.org/zh-cn/latest/docs/software/concepts/odometry.html)
